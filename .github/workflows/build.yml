name: Build Windows EXE

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Install Dependencies
      run: npm install

    - name: Build Executable
      run: npx electron-builder --windows nsis:ia32 portable

    - name: Upload EXE
      uses: actions/upload-artifact@v4
      with:
        name: TransformationStudio-Windows
        path: dist/*.exe
